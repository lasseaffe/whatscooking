
## [11:19] Scraper step granularity fix � heuristic consolidation
- Added consolidate_steps() heuristic to scripts/ingestion/scrape.py (merges adjacent micro-steps based on word count, dangling prepositions, and absence of measurement/time signals in next step)
- Wired consolidation into both parse paths in scrape.py: ecipe-scrapers instructions_list() path and ld+json fallback path
- Fixed pipeline/lib/scrape.py: removed aggressive .\s+ sentence-split, replaced with \n+-only split + consolidate_steps()
- Wired consolidation into Playwright ld+json path in pipeline/lib/scrape.py
- Created scripts/backfill-consolidate-steps.mjs � dry-run by default, --write to apply, logs diff to logs/backfill-consolidate-YYYY-MM-DD.md
- Files changed: scripts/ingestion/scrape.py, pipeline/lib/scrape.py, scripts/backfill-consolidate-steps.mjs (new)

## [Session] Cooking OS Sprint — UX/Motion System Overhaul

### What was done
- **Thread 1 — Atmospheric Depth Layer:** Added `.wc-zone-home`, `.wc-zone-discovery`, `.wc-zone-cooking`, `.wc-zone-pantry`, `.wc-zone-plans` CSS classes to `globals.css` using layered radial gradients. Applied to dashboard, discover, pantry, and cooking mode pages — replaces flat `#0d0d0c` void with zone-specific ambient warmth.
- **Thread 2 — Spring Motion System:** Created `src/lib/motion.ts` with `springTight`, `springGentle`, `springBounce` presets. Created `src/components/ui/spring-button.tsx` — drop-in `<button>` replacement with spring-physics press compression (scale + shadow, not opacity). Upgraded `recipe-card.tsx` with `whileHover` parallax depth (card at 1.015 scale, image independently at 1.04).
- **Thread 3 — Cooking Mode Redesign:** Converted scrollable `overflow-y-auto` center column to non-scrolling, vertically centered cockpit layout. Step body typography upgraded from `1.15rem` → `clamp(1.4rem, 2.5vw, 2rem)`, heading from `clamp(2rem,5vw,3.5rem)` → `clamp(2.5rem,6vw,4.5rem)`. Added `AnimatePresence` step transitions (spring enter/exit). Timers near completion (<30s) now pulse with warm color shift via `motion.div`. Added `useSwipe` hook for swipe-left/right gesture navigation. Wired `SpringButton` into Prev/Next nav.
- **Thread 4 — Mood Navigation:** Created `src/components/mood-selector.tsx` with 8 moods (Cozy, Lazy, Date Night, Comfort, Protein Hit, Adventurous, Quick Win, Impress). Each mood maps to filter presets (maxTime, tags, minProtein). Wired into `discover-client.tsx` with mood state, filter logic in `useMemo`, rotating search placeholders on focus.

### Files changed
- `src/app/globals.css` — atmospheric zone classes
- `src/lib/motion.ts` — NEW spring config
- `src/components/ui/spring-button.tsx` — NEW
- `src/lib/hooks/use-swipe.ts` — NEW
- `src/components/mood-selector.tsx` — NEW
- `src/components/recipe-card.tsx` — parallax hover upgrade
- `src/app/(app)/recipes/[id]/cooking-mode-screen.tsx` — full cockpit redesign
- `src/app/(app)/discover/discover-client.tsx` — mood selector + rotating placeholder
- `src/app/(app)/pantry/pantry-client.tsx` — zone class
- `src/app/(app)/dashboard/page.tsx` — zone class

## [Session] Tier 2 — Gamification UI + Pantry Waste Reduction

### Cooking History Widget
- Created `src/app/(app)/dashboard/cooking-history-widget.tsx` — server component
  - Queries `streak_events` (action_id=recipe_cooked) for last 30 days
  - Renders 30-day heatmap (orange gradient cells for cooked days)
  - Calculates consecutive-day streak with today-grace logic
  - Shows streak badge if streak > 1 day
  - Returns null if no events (graceful degradation)
- Wired into `src/app/(app)/dashboard/page.tsx` (after Trending this week, before Cookbooks)

### Achievements Widget
- Created `src/app/(app)/dashboard/achievements-widget.tsx` — server component
  - Queries recipe_saves + own recipes to build AchievementContext
  - Runs `computeBadges()` from `src/lib/achievements.ts`
  - Shows tier summary pills (bronze/silver/gold counts) + 5 most recent earned badges
  - Links to /world-cup-2026 for full badge view
  - Returns null if no badges earned (graceful degradation)
- Wired into `src/app/(app)/dashboard/page.tsx`

### Pantry Waste Reduction ("Use Soon")
- Created `supabase/migrations/20260518b_pantry_expiry.sql` — adds `expires_at date` column + index
- Created `src/app/api/pantry/use-soon/route.ts` — GET endpoint returning recipes using items expiring ≤5 days
- Created `src/app/(app)/pantry/use-soon-section.tsx` — client-side horizontal scroll card carousel
- Wired `<UseSoonSection />` into pantry-client.tsx

### Typography (Fraunces font)
- Added Fraunces Google Font to `src/app/layout.tsx` with axes ["opsz","SOFT","WONK"]
- Applied `var(--font-fraunces)` to dashboard, cooking mode, discover hero, and all h2/h3 headings throughout

### Files changed
- src/app/(app)/dashboard/cooking-history-widget.tsx (new)
- src/app/(app)/dashboard/achievements-widget.tsx (new)
- src/app/(app)/dashboard/page.tsx (import + wire both widgets)
- src/app/(app)/pantry/use-soon-section.tsx (new)
- src/app/api/pantry/use-soon/route.ts (new)
- supabase/migrations/20260518b_pantry_expiry.sql (new)
- src/app/layout.tsx (Fraunces font)

## [14:30] Task 3: Create GET /api/profiles/[username] route
- Implemented public profile API endpoint at `src/app/api/profiles/[username]/route.ts`
- Route fetches profile data (id, username, full_name, avatar_url, bio, created_at) with username lookup
- Aggregates stats: follower_count, following_count, cook_count, recipe_count, cookbook_count using Promise.all()
- Checks if requesting user is following the profile
- Returns 404 if profile not found
- Uses Next.js 15+ async params pattern: `{ params }: { params: Promise<{ username: string }> }`
- Exports `dynamic = "force-dynamic"` for fresh social data on each request
- TypeScript validation: no new errors introduced
- Files changed: `src/app/api/profiles/[username]/route.ts` (new)

## [14:11] Social Layer Tasks 16–18 complete

- **Task 16** — CookingMode share flow: added `CookPostSheet` import to cooking-mode-screen.tsx, wired `donePhase="share"` on "Submit & finish" and "Back to Homepage" clicks, added share phase JSX block that renders `CookPostSheet` with close/success → `onExit()`
- **Task 17** — Username links: added optional `creatorUsername` prop to `RecipeCard` with "by @username" link to `/profile/[username]`; added `@username` creator link to cookbook detail page (already had `profiles(username)` in query)
- **Task 18** — Share activity toggle: extended `/api/user-preferences` PATCH to accept `share_activity: boolean`; updated settings page to async + fetch initial value from `user_preferences`; added `ShareActivityToggle` component with optimistic toggle UI; wired into new "Social & Activity" settings section
- Files changed:
  - `src/app/(app)/recipes/[id]/cooking-mode-screen.tsx`
  - `src/components/recipe-card.tsx`
  - `src/app/(app)/cookbooks/[slug]/page.tsx`
  - `src/app/(app)/settings/settings-client.tsx`
  - `src/app/(app)/settings/page.tsx`
  - `src/app/api/user-preferences/route.ts`

## [15:37] Task 2: useCulinarySound Hook — Web Audio Synthesis
- Created test file `src/hooks/__tests__/useCulinarySound.test.ts` with 5 test cases (TDD RED phase)
- Tests validate mute state initialization, toggleMute() behavior, localStorage persistence, mute initialization from storage, and play() no-op when muted
- Implemented `src/hooks/useCulinarySound.ts` hook with Web Audio API synthesis
- Hook exposes `play(id)`, `muted`, `toggleMute()` interface
- Synthesizes 6 culinary sounds procedurally (ceramic-tap, cloth-swipe, wooden-knock, stove-ignite, timer-drop, warm-bell)
- playOscillator() helper with frequency glide support, exponential amplitude decay
- playNoise() helper generates white noise with bandpass filter for cloth-swipe effect
- synthesize() switch statement maps SoundId to audio parameters
- Respects prefers-reduced-motion media query for accessibility
- Lazy AudioContext creation with suspended state handling (resume() for user interaction)
- Mute state persists to localStorage key `wc-sound-muted`
- All 5 tests passing
- Commits: 044019d (test), dd032f6 (feat)
- Files changed: src/hooks/useCulinarySound.ts (new), src/hooks/__tests__/useCulinarySound.test.ts (new)

## [Task 3] Create pick-recipe API route
- Created `src/app/api/onboarding/pick-recipe/route.ts` — new POST endpoint for onboarding
- Implements recipe scoring logic: dietary match (±10/−20), popularity (+saved_count), family servings (+3), easy difficulty (+2)
- Filters candidates by cookType (enthusiast/family-chef allows medium), goal (fast filters to ≤30min prep)
- Returns best recipe or `null` if no dietary match found
- Files created: `src/app/api/onboarding/pick-recipe/route.ts`
- Commit: `9fcb8a4`

## [13:45] Task 1: DB Migration — cook_along_messages
- Created migration file `supabase/migrations/20260518_cook_along_messages.sql`
- Table: cook_along_messages with uuid PK, user_id, recipe_id, role (user/assistant), content, step_index, created_at
- Added compound index on (user_id, recipe_id, created_at) for efficient lookups
- Enabled RLS with policy: users can only manage their own cook_along_messages
- Ran `npx tsc --noEmit` — no migration-related TypeScript errors
- Committed with message: "feat(db): add cook_along_messages table with RLS"
- Git SHA: dd9b540
- Files changed: supabase/migrations/20260518_cook_along_messages.sql (new)

## [Task 5] Spotlight dual-ring upgrade with warm amber pulse
- Upgraded `src/components/onboarding/Spotlight.tsx` spotlight component
- Changed overlay from `rgba(0,0,0,0.72)` to warm brown `rgba(16,10,4,0.80)` for cinematic depth
- Replaced single white ring with dual warm amber rings (#C19A6B):
  - Outer ambient glow ring: 6px offset, 0.15 opacity, subtle aura effect
  - Inner pulse ring: 1.5-2.5px stroke width, 0.45 opacity, 3-second continuous pulse animation
- Inner ring animates: strokeWidth [1.5, 2.5, 1.5] over 3s repeat infinite with easeInOut, positioned within target element bounds
- Both rings spring-animate to follow spotlight target with stiffness=300, damping=30
- Commit: `1c98417`
- Files changed: `src/components/onboarding/Spotlight.tsx`
