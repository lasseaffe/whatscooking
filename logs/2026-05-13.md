# 2026-05-13

## [WC] Cooking-mode typewriter on step text
- Wired existing `TypewriterText` into the step heading in `cooking-mode-screen.tsx` (re-types per step via `key={`heading-${step}`}`, speed 36, golden caret #E8C97A).
- Replaced plain `<p>` for step body with `motion.p` (opacity + 6px slide, 0.25s delay so it appears just after heading starts) — preserves `AnnotatedText` glossary click targets that character-typing would have broken.
- Imported `motion` from `framer-motion` (already installed).
- Did NOT add the `text-three.tsx` snippet — duplicate of existing `TypewriterText` and weaker (hardcoded text, no caret, no reset).
- Side-fix: rewrote `~/.claude/settings.json` to strip PowerShell `&` call-operator prefix from 9 gsd-* hook commands so they run under bash without `syntax error near unexpected token \`&\``.
- Files changed: `whatscooking/src/app/(app)/recipes/[id]/cooking-mode-screen.tsx`, `C:/Users/lasse/.claude/settings.json`.

## [WC] Reverted cooking-mode step typewriter
- User didn't like the animation on step heading/body — reverted both back to plain `<h1>` + `<p>` with `AnnotatedText`.
- Removed `framer-motion` import (TypewriterText import kept; still used for SOS tip).
- Files changed: `whatscooking/src/app/(app)/recipes/[id]/cooking-mode-screen.tsx`.

