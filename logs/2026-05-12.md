## [18:11] WC Pipeline & Polish — Admin dashboard, palettes, converter, layout hardening

- **Ingredient converter fix**: extended `convertUnit()` in `recipe-columns-client.tsx` to handle bidirectional conversion (imperial→metric via cups/tbsp/tsp/oz/lbs) and null amounts (renders as empty string, no NaN). Added `IMPERIAL_UNITS` and `METRIC_UNITS` sets for pass-through logic.
- **15 color palettes**: added 10 new palettes to `globals.css` (CSS variable blocks) and `palette-switcher.tsx` (PALETTES array). New palettes: Burgundy & Wine, Sage & Herbaceous, Charcoal + Terracotta, Teal & Saffron, The Copper Skillet, Nordic Kitchen, Umami Midnight, Matcha & Milk, Smoked Paprika, Lavender Honey.
- **Recipe layout hardening**: improved "No instructions yet" message copy, added admin-only "Needs Enhancement" badge on recipe pages where `instructions < 3 || ingredients < 3`.
- **Admin dashboard** at `/admin` (server-gated by `ADMIN_EMAIL` env var): 10-panel sidebar UI with streaming log display for long-running operations.
- **Admin panels created**: Description Enhancer, Instruction Enhancer, Ingredient Repair, Template Repair (with sparse recipe list + batch fix), Image Audit, Playwright Fixer, Duplicate Detector, Bulk Delete, Complex Issue Resolver, Palette Previewer.
- **New API routes**: `/api/admin/fix-instructions`, `/api/admin/fix-ingredients`, `/api/admin/audit-images`, `/api/admin/find-duplicates` (trigram similarity), `/api/admin/resolve-issue` (Claude AI + appends to `logs/admin-complex-fixes.log`), `/api/admin/template-repair` (GET=list sparse, POST=batch extract).
- **Extended** `/api/admin/fix-descriptions` with `scope` param (missing/all/source:standard/source:premium/source:dataset) and `x-admin-secret` auth.
- **Admin nav link** in `app-nav.tsx`: gold Settings2 icon, visible only to admin email (checked client-side via `NEXT_PUBLIC_ADMIN_EMAIL`).
- Files: `recipe-columns-client.tsx`, `globals.css`, `palette-switcher.tsx`, `recipes/[id]/page.tsx`, `app-nav.tsx`, `api/admin/fix-descriptions/route.ts`, 6 new API routes, `admin/page.tsx`, `admin/admin-client.tsx`, 12 panel files

## [17:01] Task 1: Add macro fields to template meal data

- Updated `PlanTemplate` interface meals object to include three new numeric fields: `protein_g`, `carbs_g`, `fat_g`
- Replaced entire `PLAN_TEMPLATES` array (7 templates) with realistic macro values per meal:
  - High-Protein: 28–42g protein per meal (chicken 42g, salmon 38g, tuna 44g, frittata 28g, meatballs 34g)
  - Plant-Based: 10–18g protein per meal (balanced carbs 44–88g)
  - Quick Weeknights: 14–36g protein per meal (high variance: pasta 18g, burger 36g)
  - Mediterranean: 8–46g protein per meal (hummus 8g, chicken 46g)
  - Budget-Friendly: 10–34g protein per meal (chili 16g, meatballs 34g)
  - Family Feast: 6–48g protein per meal (pancakes 6g, roast chicken 48g)
  - Meal Prep Sunday: 4–40g protein per meal (banana bread 4g, beef stew 40g)
- TypeScript verified: no errors on plan-templates.ts
- Files: `src/app/(app)/plans/new/plan-templates.ts`
- Committed: `feat(plans): add protein_g/carbs_g/fat_g to template meal entries` (commit: 3c16f37)

## [10:20] WC-5: member_meal_reactions → Discovery ranking influence

- `discover/page.tsx`: added `reactionScore()` (rating 3→+2, 2→0, 1→-3) and generic `rankByReactions<T>()` sort function
- 7th parallel query fetches `household_members` IDs for the user; a sequential follow-up fetches `member_meal_reactions` for those member IDs
- Builds a `scoreMap` (recipe_id → cumulative score) then re-sorts both `swipeRecipes` (hero swipe deck) and `gridRecipes` (all recipes grid) before passing to client
- Loved recipes rise to the top of the swipe deck; disliked recipes sink to the bottom — zero client-side changes required
- Files: `src/app/(app)/discover/page.tsx`

## [10:00] XA-4: WC recipe_cooked → HF streak events

- `cook-entry/route.ts` now inserts `{ action_id: 'recipe_cooked' }` into shared `streak_events` table after pantry deduction; best-effort (non-blocking)
- Files: `src/app/api/plans/[id]/cook-entry/route.ts`

## [10:05] XA-5: VP food export → WC shopping list auto-import

- `shopping-list-client.tsx` reads `?vpItems=` JSON param on mount, calls `addToShoppingList()` with `recipeTitle: 'VenturePath Expedition'`, then removes the param from URL
- Shows "+N from VenturePath" badge in shopping list subtitle when items were imported
- Files: `src/app/(app)/shopping-list/shopping-list-client.tsx`

## [08:10] WC-3: Pantry expiry → Meal Plan "use soon" banner

- Added `GET /api/pantry/items` route — fetches all pantry items with name, quantity, expires_at, category
- Added `expiringItems` state + `useEffect` in `PlanBuilder` that fetches pantry on mount, filters items expiring within 2 days
- Renders an amber warning row above the day cards listing the expiring item names
- Files: `src/app/api/pantry/items/route.ts` (added GET), `src/app/(app)/plans/[id]/plan-builder.tsx`

## [08:10] WC-4: Events guest count → recipe ingredient scaling

- Updated `GET /api/events/[id]/recipe-ingredients` to also fetch `servings` from the `recipes` table and include `rawQuantity` + `unit` per ingredient
- Added `scaleTo` state to `ShoppingTab` (defaults to confirmed guest count)
- Added guest count input inside the recipe import panel (shows confirmed count as reference)
- `scaleQuantity()` computes `(rawQuantity / recipeServings) * scaleTo` — quantities in the panel and in the imported items are both scaled
- Files: `src/app/api/events/[id]/recipe-ingredients/route.ts`, `src/app/(app)/events/[id]/tabs/shopping-tab.tsx`

## [07:39] WC-1: Shopping List checked item → Pantry add modal

- Added pantry prompt modal to `shopping-list-client.tsx` — appears when user checks off an item (not when unchecking)
- `handleToggle` detects direction: checking triggers `setPantryPrompt(item)`
- Modal offers "Yes, add to pantry" (POSTs to `/api/pantry/items`) or "Skip"
- Checked items added to pantry get an "in pantry" badge and 0.35 opacity
- Files changed: `src/app/(app)/shopping-list/shopping-list-client.tsx`

## [07:39] WC-2: Meal Plan "Mark as cooked" → Pantry ingredient deduction

- Created `src/app/api/plans/[id]/cook-entry/route.ts` — POST endpoint
  - Fetches recipe ingredients by `recipe_id` or title lookup fallback
  - Fuzzy-matches against user pantry (same bidirectional word logic as shopping route)
  - PATCHes matched pantry items: `quantity = max(0, currentQty - ingredientAmount)`
  - Only deducts items that have a tracked numeric quantity
- Added `ChefHat` (cook) button to `EntryRow` in `plan-builder.tsx`
  - Visible on hover, shows spinner while in-flight, turns green when done
  - `cooked` state is local — button disabled after first cook per session
  - `planId` prop added to `EntryRow` for the API call
- Files changed:
  - `src/app/api/plans/[id]/cook-entry/route.ts` (new)
  - `src/app/(app)/plans/[id]/plan-builder.tsx`

## [14:15] Task 7: Add API error boundary with TDD
- Followed TDD: wrote failing tests, confirmed module not found, implemented boundary, verified all 3 tests pass
- Created `src/lib/api-error-boundary.ts` with `withPaywallErrorBoundary(handler)` HOF
- HOF wraps async route handlers (Request, ctx?) → Promise<Response>
- On success: returns handler result unchanged (pass-through)
- On PaywallError: serializes to NextResponse.json(error.toJSON(), { status: 402 })
- On other errors: rethrows unchanged so existing error boundaries + logging continue
- Created `src/lib/api-error-boundary.test.ts` with 3 passing tests:
  1. Success: handler returns 200 OK, wrapped result passes through
  2. PaywallError: thrown error serializes to 402 with feature, tiers, upgrade_url
  3. Other errors: non-PaywallError thrown errors rethrow (preserve existing handlers)
- Test environment: set `@jest-environment node` to enable Request/Response polyfill (jsdom default insufficient for Next.js server APIs)
- Files: `src/lib/api-error-boundary.ts` (13 lines), `src/lib/api-error-boundary.test.ts` (43 lines)
- Committed: `feat(billing): add withPaywallErrorBoundary serializing PaywallError as HTTP 402` (commit: d480341)

## [13:27] Task 6: Add requireTier + hasFeature to entitlements.ts
- Followed TDD: appended 13 failing tests, confirmed failure, implemented functions, verified all 18 tests pass
- Appended to `src/lib/entitlements.ts`:
  - Added import: `PaywallError, type GatedFeature` from `./paywall-error`
  - `FEATURE_TIER` map: 10 gated features to their minimum tier (solo_plus or household)
  - `isEntitlementActive(ent, now?)`: boolean logic for status activation
    - active/trialing/grandfathered → always active
    - canceled → active only if periodEnd > now
    - past_due/free → never active
  - `TIER_RANK` map: free=0, solo_plus=1, household=2 (allows higher tiers to pass lower gates)
  - `meetsRequirement(ent, required)`: checks isEntitlementActive AND tier ranking
  - `requireTier(userId, required, feature, supabase)`: async throw PaywallError if not met, else void
  - `hasFeature(userId, feature, supabase)`: async boolean check using FEATURE_TIER[feature]
- Appended to `src/lib/entitlements.test.ts`:
  - Added imports: `requireTier, hasFeature` and `PaywallError`
  - requireTier (10 tests): tier gates, status transitions (canceled mid-period grants, past_due denies), trialing/grandfathered treated as tier, PaywallError properties preserved
  - hasFeature (3 tests): feature-to-tier mapping, solo/household access denial/grant scenarios
- All 18 tests pass (5 getEntitlement + 10 requireTier + 3 hasFeature)
- Files: `src/lib/entitlements.ts` (125 lines total), `src/lib/entitlements.test.ts` (161 lines total)
- Committed: `feat(billing): add requireTier + hasFeature with tier ranking and status semantics` (commit: 6e8b427)

## [HH:MM] Task 5: Add getEntitlement function in entitlements.ts
- Followed TDD: wrote failing test, confirmed error (module not found), implemented function, verified 5/5 tests pass
- Created `src/lib/entitlements.ts` with `getEntitlement(userId, supabase)` async function
- Function reads from `subscriptions` table, returns `Entitlement` interface with: `tier` (SubscriptionTier), `status` (SubscriptionStatus), `trialEndAt` (Date | null), `periodEnd` (Date | null)
- Returns FREE_DEFAULT entitlement ({ tier: 'free', status: 'free', trialEndAt: null, periodEnd: null }) when no subscription row exists
- Throws on database error (does not silently fall back to free)
- Converts ISO string dates to Date objects (trial_end_at → trialEndAt, current_period_end → periodEnd)
- Created `src/lib/entitlements.test.ts` with 5 passing tests:
  1. Free default for no subscription
  2. Active solo subscriber with solo_plus tier
  3. Trialing household subscriber
  4. Grandfathered household subscriber
  5. Database error throws (never silent fallback)
- Files: `src/lib/entitlements.ts` (42 lines), `src/lib/entitlements.test.ts` (67 lines)
- Committed: `feat(billing): add getEntitlement reading from Supabase subscriptions` (commit: d4bc0b0)

## [HH:MM] Task 3: Add PaywallError typed error class
- Followed TDD: wrote test first, confirmed failure (module not found), implemented class, verified 3/3 tests pass
- Created `src/lib/paywall-error.ts` with PaywallError class extending Error
- Exported `GatedFeature` type union (10 feature strings: ai_recipe_gen, ai_plan_gen, recipe_import, coach_chat, shared_pantry, household_members, dinner_parties, family_allergens, unlimited_saves, unlimited_plans)
- PaywallError properties: `requiredTier` (Exclude<SubscriptionTier, 'free'>), `currentTier` (SubscriptionTier), `feature` (GatedFeature)
- Implemented `toJSON()` method returning 402-shaped response: { error: 'payment_required', feature, required_tier, current_tier, upgrade_url }
- Created `src/lib/paywall-error.test.ts` with 3 passing tests covering: tier capture, Error instanceof checks, JSON serialization
- Files: `src/lib/paywall-error.ts` (42 lines), `src/lib/paywall-error.test.ts` (40 lines)
- Committed: `feat(billing): add PaywallError with 402-shaped JSON serializer` (commit: affc910)
- Import path corrected to use `src/lib/subscription-types.ts` (not `supabase/types` as planned)

## [HH:MM] Task 5: Add /api/nutrition-goals endpoint
- Created `src/app/api/nutrition-goals/route.ts` with GET handler
- GET handler: retrieves user via `supabase.auth.getUser()`, returns 401 if unauthorized
- Calls `getUserNutritionGoals(user.id)` to fetch goals (returns `{ calories, protein_g, carbs_g, fat_g }` with RDA defaults fallback)
- Uses `export const dynamic = "force-dynamic"` to ensure fresh data per request
- TypeScript check: one import error for `@/lib/nutrition-goals` (parallel task 2 — expected to resolve once that commits)
- All other pre-existing TypeScript errors unrelated to this endpoint
- Files: `src/app/api/nutrition-goals/route.ts` (15 lines)
- Committed: `feat(plans): add /api/nutrition-goals endpoint` (commit: 299755c)
- Status: DONE_WITH_CONCERNS — endpoint created and committed; tsc shows expected import error from parallel task

## [HH:MM] Task 2: Add TypeScript types for Subscription domain
- Created `src/lib/subscription-types.ts` with comprehensive subscription type definitions
- Exported types: `SubscriptionTier`, `SubscriptionStatus`, `BillingPeriod`, `SubscriptionEventSource`
- Added database row interfaces: `SubscriptionRow` (user_id, rc_user_id, tier, status, period tracking, stripe/RC IDs, billing_period, timestamps) and `SubscriptionEventRow` (id, user_id, event_type, source, payload, created_at)
- Included constant arrays for each enum type (SUBSCRIPTION_TIERS, SUBSCRIPTION_STATUSES, BILLING_PERIODS, SUBSCRIPTION_EVENT_SOURCES)
- Types match migration schema from `supabase/migrations/20260511000000_subscriptions.sql`
- TypeScript compilation verified: no new errors; pre-existing errors in recipes/page.tsx and app-nav.tsx unrelated to subscription types
- File: `src/lib/subscription-types.ts` (75 lines)
- Committed: `feat(billing): add SubscriptionTier/Status/Row types` (commit: 8c683c0)
- Ready for Task 3: `src/lib/paywall-error.ts` can now import these types

## [15:20] Meal Plan Creator & Editor Redesign

- Rewrote `plans/new/page.js`: carousel hero (3 cards, arrow nav, translateX animation), always-visible inline form (name + days + meals/day), dietary chips, "Set your goals →" nudge chip
- Added `protein_g`/`carbs_g`/`fat_g` to all 35 meals across 7 plan templates in `plan-templates.ts`
- Created `src/lib/nutrition-goals.ts`: `getUserNutritionGoals()` reads `calorie_goals` table, parses macro targets from `goal.notes` JSON, falls back to RDA defaults
- Created `src/app/api/nutrition-goals/route.ts`: GET endpoint returning user goals
- Updated `template-card.tsx`: macro progress bars (kcal/protein/carbs/fat vs goals) with color logic (green ≥90%, orange ≥60%, amber >110%)
- Updated `plan-builder.tsx`: week-average macro bar pinned below plan header + per-day kcal/protein footer per day column
- Updated `api/plans/route.ts`: POST now persists `nutritional_goals`, `duration_days`, `meals_per_day`
- 8 commits: 3c16f37 → e5d11f0
