# 2026-05-19 — Meal Planner (Plans 3, 4, 5, 6)

## [14:52] WC: ModalContent implementation — header, stat pills, dietary tags
- Replaced `ModalContent` stub in `RecipeDetailModal.tsx` with full implementation
- Added cuisine label (saffron accent, uppercase tracking)
- Added title (Libre Baskerville, warm linen) + description (italic muted brown)
- Stat pills grid (4 columns): Time, Calories, Serves, Cuisine
- Dietary tags (rounded pills, flexbox wrap)
- Fixed TypeScript operator precedence error (mixed `??` and `||`)
- Files changed: `src/components/plans/RecipeDetailModal.tsx`
- Commit: ebb26df

## [02:55] Creation flow modernization + orphan cleanup (Plan 6)

Branch `feat/meal-planner-pinboard-weave-create`. The last big piece of meal-planner debt.

### What landed
- `/plans/new` is now a TypeScript page that always redirects to the Pinboard at `/plans/[id]`. The inline `MealPlanDndBuilder` branch is gone — templates and custom plans both land in the new model.
- `POST /api/plans` accepts optional `template_meals: string[]` and `pinboard_filters: object`. Template meal titles are matched against the recipe DB (exact + `ilike` fallback) and bulk-inserted into `meal_plan_pins` so a templated plan opens with real pins, not empty placeholders.
- Template cards repainted in the Pinboard token palette — Ember on Midnight, Sandstone borders, image fallback to emoji glyph. Removed the rainbow `accentColor`/`gradient` overrides.
- Retired ~970 lines of legacy code: `RecipeBank.tsx`, `WeeklyPlanGrid.tsx`, `dnd-builder.tsx`, and the `scratch/page.js` stub.

### Tests
- Solver: 39/39
- Typecheck clean on every new/touched file

### What's left (Plan 7 candidates if useful)
- Per-member breakdowns in recipe detail pages
- Member-meal-reaction feedback loop into solver scoring
- Migrating `plan-templates.ts` to a DB-backed table

---

## [02:35] Squad-aware filters + pantry-band data (Plan 5)

Branch `feat/meal-planner-pinboard-weave-squad`.

### Squad-aware
- `src/lib/plans/squad-resolve.ts` collects members + avoid/dislike/love from `member_ingredient_preferences` and `member_allergens` across the user's kitchen groups (uses the canonical `kitchen_group_members` join discovered via grep in `/api/family/members`).
- `GET /api/household/squad-preferences` returns the resolved squad shape.
- `recipeMatch.squadHardFilter` excludes recipes containing any avoid term as a substring of any ingredient.
- `recipeMatch.squadScore` blends ±love/dislike weight; mixed into `inspiration_match` in the weave route, and as `score * 0.3` in the picker route — no solver schema change.
- Weave + picker routes honor a `squad_aware` toggle (defaults on when ≥1 member exists; per-plan opt-out via `pinboard_filters.squad_aware`).
- ConstraintChipBar `Squad` chip now opens a popover with a member list, tag pills (avoid red / dislike yellow / love green), an "Apply preferences" toggle, and the existing portions slider beneath.
- PinboardFeed fetches squad data once and renders a 👥 warning badge on cards containing dislike ingredients.

### Plan 4 follow-up
- Weave route now returns `pantry_match` per recipe in the `recipes[]` payload.
- WeaveSection threads it to WeaveGrid; the density ribbon's pantry band finally lights up with real data.

### Tests
- Solver: 39/39 passing
- Typecheck clean on touched files

### Deferred (Plan 6 candidates)
- Per-member breakdowns inside individual recipe pages.
- Member-meal-reaction feedback loop into the solver.
- `/plans/new` migration to the Pinboard model (would unlock the RecipeBank / WeeklyPlanGrid cleanup).

---

## [02:15] Polish + smart features shipped (Plan 4)

Branch `feat/meal-planner-pinboard-weave-polish-smart` stacked on the cook view.

### What landed
- **Smart-swap rebalance:** picker now ranks around adjacent-day cuisines / proteins / dish types. Banner shows "Picking around: …" so the user understands the bias.
- **Anti-repeat tension:** new `src/lib/weave-solver/tension.ts` pure helper (5 unit tests). Yellow underline + hover tooltip on woven cells that share cuisine / shared dish type / protein keyword with an adjacent-day same-meal cell.
- **Drag-and-drop:** cell-to-cell swap in the Weave grid via @dnd-kit. PointerSensor activation distance keeps click-to-open-picker working.
- **Batch-cook 2× badge:** on cook-day cells whose recipe reappears as a leftover. Same hint on TodayCard via a recipe-id heuristic (cook ≥ 1 AND reheat ≥ 1).
- **Density ribbon:** new `DayDensityRibbon` shows three thin bands per day (cook time / leftover / pantry coverage). Hides when all bands are near-zero. Pantry pct currently always 0 in v1 because the weave route doesn't yet thread per-recipe pantry_match into the response — non-blocking polish for a follow-up.

### Solver tests
- 34 → 39 passing (`tension.test.ts` adds 5)
- Full suite: 8 files, 39 tests, all green

### Skipped (justified)
- Phase E orphan cleanup. `RecipeBank.tsx`, `WeeklyPlanGrid.tsx`, `dnd-builder.tsx` are still imported by `src/app/(app)/plans/new/page.js` (the plan creation wizard). Per the plan's escape hatch, they stay until the create-plan flow is migrated to the new Pinboard model.
- Pantry-band data path (weave route extension). Density ribbon shipped without it, gracefully shows 0% until the route lands.

### Deferred to Plan 5
- Squad-aware filters (touches household_member_preferences — warrants own scoping).
- Moodboard token entries (depends on master state).

---

## [00:36] /cook execution view shipped (Plan 3)

Cook view for the meal planner is on `feat/meal-planner-pinboard-weave-cook` (stacked on the UI plan branch).

### What landed

- **Migration:** `meal_entries.cooked_at timestamptz` (nullable) + index on `(meal_plan_id, cooked_at)`.
- **API:**
  - `POST/DELETE /api/plans/[id]/entries/[entryId]/cook` — mark/unmark cooked, auto status transitions `woven` → `cooking` → `completed`, emits a streak event (no-op stub today).
  - `GET /api/plans/[id]/shopping` — pre-existing, consumed by the new ShoppingPanel.
- **`/plans/[id]/cook` route:**
  - `CookHeader` — title + status + progress bar (`X / Y cooked`)
  - `TodayCard` — hero card surfacing today's meals (computed from `week_start + duration_days`), big CookCheckButton per entry
  - `DayStack` — collapsible rows for upcoming days, compact CookCheckButton
  - `ShoppingPanel` — sidebar with category-grouped "to buy" list, collapsed "already have", `⚠ recipes without ingredients` warning
- **Cross-app hook:** `src/lib/streak-emit.ts` is the integration point. Currently logs to console in dev; replace its body when HolyFlex streak surface lands in WC.

### Out of scope
- Cooked badge wiring on the Weave grid (defensive marker added, TODO logged).
- Cook-day reminders, calendar export, in-recipe cooking-mode hand-off, post-cook ratings.
- Real HF streak integration (stub in place, ready to swap).
- Persistent shopping check-off state across sessions.

### Branch state
- Solver regression test pass: **34/34**.
- Typecheck clean on every new/modified file.
- 5 commits on top of `feat/meal-planner-pinboard-weave-ui`.
## [02:25] WC: cookbooks.com purge + re-pull pipeline
- 15,000 cookbooks.com recipes found in DB - bot-blocked site, unrecoverable images
- Built scripts/ingestion/repull.py (DDG + recipe_scrapers + qwen3:8b AI fallback)
- Built scripts/repull_cookbooks.py orchestrator
- qwen3:14b/8b too slow on user's hardware for batch AI generation
- Decision: just DELETE all 15k cookbooks.com recipes
- Deletion running in background, 100-row batches, ~14min ETA

## [02:33] WC: cookbooks.com purge complete
- 14,890 cookbooks.com recipes deleted in 414s (~7min) via 100-row batches
- DB now contains only modern scraped recipes + dataset entries with working sources
- Schema cache transiently lagging post-delete (self-heals)
- All scripts committed (focal_point.py, fix_images_bulk.py, repull.py, repull_cookbooks.py)

## [00:05] WC nav-covers-content fix via /debug
- Root cause: layout had inline `style={{ marginLeft: "70px" }}` on .wc-main-content; inline styles win over the CSS rule `body[data-nav-expanded="true"] .wc-main-content { margin-left: 320px }`, so content was frozen at 70px and the expanded 320px nav covered the 70-320px slice
- Fix: removed the inline marginLeft; moved the default 70px into the CSS rule so both states (70px collapsed, 320px expanded) flow through the same selector chain and the body data-attribute toggle actually wins
- Files changed: src/app/(app)/layout.tsx, src/components/app-nav.tsx

## [05:08] WC: Pass 1 image fix complete
- Re-scraped 750 recipes' images via Playwright + saliency focal point
- 143 unrecoverable (genuinely dead source URLs)
- 888 dataset entries skipped (no source_url - cannot be re-scraped)
- 84% success rate on the actionable set
- Combined with earlier Pass 2 (607 fixes), DB now has ~1,357 recipes with proper images + AI focal points
- Dedup guard caught multiple soft-block contamination attempts in real time

## [13:03] WC: Fix recipes page TypeError (total undefined)
- total prop not passed to AllRecipesClient from RecipesPage
- Added total={list.length} to JSX call
- Files changed: whatscooking/src/app/(app)/recipes/page.tsx


## [12:xx] WC: Pinboard collapse + recipe search bar + category carousels
- Created `RecipeSearchBar.tsx` — autocomplete against `/api/recipes/autocomplete` (debounced 200ms), keyboard nav (arrows/Enter/Escape), inline pin toggle
- Rewrote `PinboardFeed.tsx` — 3 parallel-fetched horizontal carousels (Breakfast/Lunch/Dinner), portrait cards (120×88px), scroll-snap
- Rewrote `Pinboard.tsx` — sticky collapsed header with pin thumbnails + search bar + reweave ↺ + expand ▾; auto-collapses on first weave; WeaveGrid is now dominant visual after weaving
- Files changed: `src/components/plans/Pinboard.tsx`, `src/components/plans/PinboardFeed.tsx`, `src/components/plans/RecipeSearchBar.tsx` (new)

## [14:50] WC: New plan page layout reorder + template carousel
- Moved Custom settings + plan name + Create button above template section
- Converted template CSS grid to single horizontal scroll row (flex overflow-x-auto)
- Added prev/next chevron buttons with atStart/atEnd scroll tracking
- Negative margin bleed (-mx-4 sm:-mx-6) for edge-to-edge card scrolling
- Card width fixed at w-[280px] flex-shrink-0; scrollbarWidth none for Firefox compat
- Files changed: src/app/(app)/plans/new/page.tsx

